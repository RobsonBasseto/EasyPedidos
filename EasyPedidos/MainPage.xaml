<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
             x:Class="EasyPedidos.MainPage"
             BackgroundColor="#FFFFFF">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,50,30,30"
            Spacing="30"
            VerticalOptions="CenterAndExpand">

            <!-- LOGO + TÍTULO -->
            <VerticalStackLayout Spacing="20" HorizontalOptions="Center">
                <Image
                    Source="logo.png"
                    HorizontalOptions="Center" />
            </VerticalStackLayout>

            <!-- AÇÕES -->
            <VerticalStackLayout Spacing="10">

                <!-- NOVO PEDIDO — TUDO DIRETO -->
                <syncfusion:SfButton
                    x:Name="AddPedido"
                    Text="Novo Pedido"
                    Clicked="OnNovoPedidoClicked"
                    Background="#FF6B35"
                    TextColor="White"
                    FontAttributes="Bold"
                    FontSize="18"
                    CornerRadius="12"
                    HeightRequest="60"
                    Padding="20,0"
                    ImageSource="plus_circle_white.png"
                    HorizontalOptions="FillAndExpand"
                    Margin="0,10" />

                <!-- VER PEDIDOS — TUDO DIRETO -->
                <syncfusion:SfButton
                    x:Name="ListPedidos"
                    Text="Ver Pedidos"
                    Clicked="OnListarPedidosClicked"
                    Background="#F5F5F5"
                    TextColor="#FF6B35"
                    FontAttributes="Bold"
                    FontSize="18"
                    CornerRadius="12"
                    HeightRequest="60"
                    Padding="20,0"
                    ImageSource="list_orange.png"
                    HorizontalOptions="FillAndExpand"
                    Margin="0,10" />

            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>